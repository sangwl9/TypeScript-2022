const sangwoo = {
    nickname: "nick",
};

// sangwoo.hello();
// [1, 2, 3, 4] + false;

// function divide(a, b) {
//     return a / b;
// }

// divide("asdas");

const player = {
    age: 12,
};

// player.age = false;
